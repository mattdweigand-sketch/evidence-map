# /run

Start a evidence-map workflow for a source folder.

Expected shape:

```text
/run <name> <kind> <input-folder>
```

Use:

```bash
npm --prefix .system run run -- --name "<name>" --kind <deck|workbook|document|report|mixed> --input <input-folder>
```
