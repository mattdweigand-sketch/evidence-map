# truth-layer-os

Read `AGENTS.md` first.

Implementation lives under `.system/`. Read `.system/docs/mcp.md` for the MCP adapter boundary and `.system/docs/architecture.md` for the store and workflow boundary.
